galaxy_info:
  author: Björn Albers
  description: Install Munki server on Ubuntu Linux
  license: MIT
  min_ansible_version: 2.4
  platforms:
    - name: Ubuntu
      versions:
        - all
  galaxy_tags:
    - munki
    - macos
    - mac
    - osx
    - server
    - http
    - samba
    - smb

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
